# en20k App 设计文档

> status: proposal v1 · 2025-03 · 源自 `english-active-20k` skill 套件（8 skill + srs.py + data/）

---

## 0. 一句话

**一个逼你"只产出"的日常训练器，加一本会自己排期的语块账本。**

---

## 1. 产品论证

### 1.1 反题：所有词汇 App 都在练识别

看英文→想中文（Anki / 百词斩 / 多邻国），练的是识别，不是激活。en20k 的铁律反方向：

> 永远是「想表达的意思 → 产出英文」。

这是产品存在的唯一理由。UI 的每个决定都要回答：**这个界面是在帮用户产出，还是在引诱用户识别？** 任何让用户"舒服地看"的设计都是背叛——包括漂亮的卡片正面、包括无限滑动的词表浏览。

### 1.2 它不是 Anki + 提示词，原因有三

1. **题型是生成的**：情境产出题的中文情境必须贴合用户真实场景（standup、code review、聊昨晚的剧），题库养不出来。
2. **评分是开放的**：用户说出/写出一句话，要判断 0-3 分并诊断失败类型（检索失败/搭配错误/语域错误），规则引擎做不到。
3. **闭环在训练之外**：speaking 的 TOT 记录、upgrade 的泛词密度、immersion 的卡壳点，都要流回同一个 bank。Anki 的数据模型装不下。

### 1.3 三条产品原则（从 skill 纪律直接翻译）

| Skill 里的纪律 | App 里的原则 |
|---|---|
| 方向永远是产出 | 没有"看卡片认词"模式，一张都没有 |
| 每天最多 15 分钟，到点就停 | **时间盒是功能**。练完的应用状态是"劝退"：今日完成，明天见 |
| 盯数据不盯感觉 | 复盘只出数字；低于红线（5/7）时报告只谈那一件事 |
| 不收已会的，激活比囤积重要 | 收割必须过"认识但用不出？"确认 + 当场造句，没有一键批量入库 |

---

## 2. 从 8 个 skill 到 3 个界面 + 1 个引擎

8 个 skill 折叠，不是 8 个功能入口。**bank 是心脏**：所有训练要么喂它（bank/immersion/speaking-TOT/upgrade），要么吃它（drill/paraphrase）。

```
                    ┌─────────────┐
   收割通道 ───────▶│  语块库 bank │───────▶ Drill（每日核心）
   · 贴字幕/文本    │  唯一事实源  │───────▶ Paraphrase（隔天）
   · speaking TOT   └──────┬──────┘
   · upgrade 升级对        │ stats
   · immersion 卡壳        ▼
                     复盘 / 体检（周报·月测）
```

| Skill | App 去处 |
|---|---|
| drill | **今日** 的主训练块（核心循环，锚点） |
| speaking | 今日 的次训练槽（隔天轮换）+ 语音输入 |
| paraphrase | 今日 的次训练槽（与 speaking 轮换） |
| upgrade | 周末槽（写一篇真东西） |
| immersion | 兴趣附加：看后 10 分钟收尾入口 |
| bank | **库** 界面 + 各训练内嵌的收割流 |
| audit | **复盘** 里的月度流程（分次 ≤20min） |
| coach | 不需要用户"跑"——周报自动从日志汇总 |

三个 Tab：**今日 / 库 / 复盘**。够了。

---

## 3. 引擎边界：什么是确定性的，什么是 LLM 的

这是架构上最重要的一个决定。

### 确定性核心（永不经过 LLM）
- **SRS 排期**：原样移植 `srs.py` 的 SM-2 变体（3 分间隔增长、2 保持、1 重来、0 记 lapse、连三 3 → active）。它已经写好、纯 stdlib、可直接复用。
- 到期队列、库存统计、趋势、红线判断。
- **bank.json 保持唯一事实源**，schema 完全兼容现有文件（超集扩展，不删不改 agent 用到的字段）。App 与 agent skill 双向读写同一份文件——"你的数据是你的"是卖点，不是姿态。

### LLM 边缘（产品差异所在）
- 情境生成（题型 A 的中文情境，贴用户场景标签）
- 开放产出的评分 + **失败类型诊断**
- speaking 的对手戏角色（会打断、追问、反对）
- upgrade 三层审稿、harvest 候选标注

原则：**排期是算法的事，不交给模型；语言判断是模型的事，不假装是规则。** Skill 文件里的诊断标准（检索/搭配/语域三分法、拐杖词清单、语域规则）提炼成版本化的 prompt 库，当作产品逻辑维护，不当作装饰。

延迟处理：drill 评分要即时——先本地启发式预判（语音转写命中目标语块→大概率 3），LLM 诊断异步补充，用户一键确认或改判。

---

## 4. 关键界面与交互

### 4.1 今日 = 一次排好序的训练，不是 dashboard

打开 app 只有一件事：开始 drill。顺序本身是信息（先 drill 后其他，核心不可断）。

```
┌────────────────────────────────┐
│ 今日 · 周四              3/13  │
│                                │
│  ┏━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│  ┃ 到期 14 卡               ┃  │
│  ┃ Drill · 产出式复习 10min ┃  │
│  ┃               [ 开始 → ] ┃  │
│  ┗━━━━━━━━━━━━━━━━━━━━━━━━┛  │
│                                │
│  今天第二件事 · 15 min          │
│  ┌────────────────────────┐  │
│  │ Speaking · code review  │  │
│  │ 场景：测试覆盖率之争     │  │
│  └────────────────────────┘  │
│                                │
│  看剧了？ 10 分钟收尾 →        │
│                                │
│  已激活 218 / 423 · 明日 14 到期│
└────────────────────────────────┘
```

- 次训练槽由周计划自动轮换（工作日 speaking/paraphrase 隔天，周末 upgrade + 复盘）——coach 的排程逻辑产品化。
- **启动成本是硬指标：从点开 app 到第一张卡 < 3 秒。** 通知 → 点开 → 已在卡 1。冷启动、登录、加载页都为这个让路。

### 4.2 Drill 舞台：全屏单卡，导航消失

训练中底部 Tab 淡出——专注纪律直接做进布局。

三种题型按 50/25/25 随机，难度阶梯恰好是"目标语块可见度"递减：

**A · 情境产出**（语块可见，练用法）
```
│  A · 情境产出              4/10 │
│                                  │
│  站会上想说：迁移又延期了，       │
│  这周先不合并，先修数据问题。     │
│                                  │
│  ⌁ push back on                  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 用它说一句英文…             │  │
│  └────────────────────────────┘  │
│  [🎤 按住说]  或打字              │
```

**B · 搭配补全**（半可见，练搭配）
```
│  B · 搭配补全                     │
│                                  │
│  a ┄┄┄┄┄ learning curve           │
│                                  │
│  补全，并用它造一句                │
```

**C · 快速反应**（不可见，练裸检索——5 秒计时条）
```
│  C · 快速反应      ▮▮▮▯▯ 5s      │
│                                  │
│  「不假思索地就能说出」           │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 说出这个意思的英文语块       │  │
│  └────────────────────────────┘  │
│                                  │
│  想不起来 →                       │
```

**揭晓时刻（本应用的招牌交互）**：

```
│         ██████████████           │
│    →  off the top of my head     │
│      （荧光笔划过，字成形）        │
│                                  │
│  3 脱口而出   2 有磕绊            │
│  1 提示后才想起   0 没想起        │
```

评分后进入**失败诊断**（0-1 分的卡，这是精华，别的 App 没有）：

| 诊断 | App 的反应 |
|---|---|
| 检索失败 | 亮出语块 + 释义 + 一个例句 → **换一个新情境，当场再造一句**（第二次产出） |
| 搭配错误 | 并排对比错/对搭配（`make a call / take a call`）→ 重建那句话 |
| 语域错误 | 语域标注（口语/中性/正式）+ 一个反例 → 用对的语域重说 |

LLM 判分 + 诊断，用户一键确认或改判（校准权留给人）。

### 4.3 时间盒与"劝退"时刻

10 卡或 15 分钟（先到为准），到点即停。完成屏是设计过的品牌时刻：

```
┌────────────────────────────────┐
│                                │
│         今日 10 卡。            │
│                                │
│   明日到期 14 · 平均间隔 +2.1d  │
│   本周 5/7                     │
│                                │
│         到点就停。               │
│                                │
└────────────────────────────────┘
```

不做连击、不做排行榜、不做"再练一组"。**进步的视觉化用"退休进度"替代 streak**：你的 423 个语块里 218 个已激活，平均间隔 19 天——间隔在变长，就是你变强的方式。

### 4.4 库 = 活的索引

不是卡片墙，是词典式的密集列表。每行的排版编码它的生命状态：

```
┌────────────────────────────────┐
│ 语块库          423 · 218 active│
│ [全部 | 到期 | 顽固 | 本周新收]  │
│ ────────────────────────────── │
│ push back on          phrasal   │
│   对…提出异议          iv 21d ● │
│ ────────────────────────────── │
│ off the top of my head  idiom   │
│   不假思索地            iv 1d ◌ │
│ ────────────────────────────── │
│ grueling               single   │
│   累死人的            iv 3d ◌   │
```

- ● 已激活（实心、字清晰）/ ◌ 学习中（虚线下划线、字略淡）——即视觉系统的"检索成形"态（见 §5）。
- `iv 21d` 用等宽字体：机器事实。
- 顽固块（lapse ≥3）行首有警示标记，coach 会在 speaking 里给它设计使用机会。

### 4.5 收割流（bank 的入库通道）

贴字幕/小说段落/邮件 → LLM 标出候选（高频 + 用户用得上的场景）→ **逐条问"这个你会自己用出来吗？"** → 确认"认识但用不出"才收 → 当场造 my_example（造错不悄悄改，指出问题让用户重造）→ 入库。

每次 8-12 条，每周 ≤60，超了警告（囤积警告是功能）。从 speaking TOT、upgrade 升级对、immersion 卡壳点转来的候选，走同一条流。

### 4.6 复盘 / 体检

- **周报自动生成**，≤40 行，数字优先。低于红线 5/7 时，整份报告只谈这一件事（coach 纪律产品化）。
- 断卡回归不羞辱：只问一个问题——"哪天断的，那天发生了什么？"（一次性，问完就翻篇）
- **月度体检**（audit）：分 2-3 次会话、每次 ≤20 min。产出激活热力表（各频段 passive vs active 横条图）+ 与上月逐项对比。这是全 app 唯一的图表页。
- 核心指标只盯四个：**激活率、泛词密度、drill 执行天数、顽固块 Top5**。其余一律不画图。

---

## 5. 视觉系统

### 5.1 方向论证

排除三种 AI 默认脸（米色衬线赤陶 / 近黑酸性绿 / 报纸排印）。从主体真实世界取材——一个中国程序员学习者的桌面：**荧光笔、字幕、词典条目、等宽字体的终端**。

核心隐喻一句话：**激活 = 检索成功 = 从模糊到清晰**。tip-of-the-tongue 是这个产品对抗的现象，就让"成形/resolve"成为统一的视觉动作。

### 5.2 Token

**色**（4+1，每个都有工作，无装饰色）：

| token | 值 | 用途 |
|---|---|---|
| `--paper` | `#FAFAF8` | 冷调纸白底（复印纸，不是米色） |
| `--ink` | `#191C20` | 正文墨色 |
| `--marker` | `#FFE34D` | 荧光笔黄——**只在检索成功揭晓时划过**，及库中 active 态的实心点。学习者的最基本激活工具 |
| `--alarm` | `#C2402C` | 只用于：到期红线、顽固块、囤积警告 |
| `--graph` | `#2E5E8C` | 只用于复盘/体检的图表线 |
| 灰阶 | `#5A6068` / `#E5E7E4` | 次级文字 / 发丝线 |

黄底配墨字对比足够；黄色永不作文字色。

**字体三原则——用字形编码文本的性质**：

| 层 | 字体 | 出现处 |
|---|---|---|
| **语言层**（中英句子的内容本身） | Spectral（衬线，含斜体例句）；中文用霞鹜文楷屏幕版（成本高则回退系统苹方） | 语块、例句、情境句 |
| **界面层** | IBM Plex Sans | 按钮、标签、导航 |
| **机器层**（排期、间隔、计数、日期） | IBM Plex Mono | `iv 21d`、`4/10`、统计数字 |

衬线=语言，无衬线=界面，等宽=机器事实。用户不需要知道这条规则，但会感到秩序。

**布局**：移动优先（每日 drill 和语音练习都发生在手机上；桌面承载贴长文本的 harvest/upgrade）。训练舞台全屏无导航；今日页是纵向跑道而非宫格。

### 5.3 Signature：「检索成形」

- 待激活语块 = 虚线下划线 + 淡墨（可感知但不完整）；
- 揭晓/答对 = 荧光笔从左划过、字从虚到实。全 app 只有这一处动效高潮；
- `prefers-reduced-motion`：退化为透明度一步切换，无划动无模糊。
- 刻意不做：环境动画、粒子、卡片 3D 翻转。一天用一次的东西，克制是尊敬。

### 5.4 自我批评（Chanel 检查）

- 荧光黄会不会廉价？→ 收缩使用面：只出现在揭晓一刻 + active 实心点，别处不见黄色。✓
- 首页会不会沦为清单 dashboard？→ 它是**有序跑道**（顺序即信息：核心训练永远第一、最大），无九宫格无小卡片阵。✓
- 移除一件配饰：原计划给"今日完成"屏加彩带动效——删。数据 + "到点就停"三个字足够有性格。✓

---

## 6. 「好用」的硬指标（验收线）

1. 点开 app → 第一张卡 **< 3 秒**（含冷启动）
2. 每日核心循环（drill 10 卡）**零配置**开跑；bank 为空时引导先收割一轮（skill 原逻辑）
3. 语音输入：按住说，松手转写，转写可改（说错了字能修，评分看内容）
4. 时间盒到点自动结束，重新进入不追加
5. bank.json 双向兼容：agent 改完 app 能读，app 导出 agent 能用（`srs.py due` 直接跑通即验收）
6. 断连可用：drill 排期本地计算，LLM 失败时降级为自评 + 事后补诊断

---

## 7. MVP 切法

**Phase 0（先跑通核心循环，2 周量级）**
- Drill 舞台（三题型 + 评分 + 失败诊断 + 时间盒）
- bank.json 读写（复用 srs.py 逻辑）+ 库的只读索引视图
- 贴文本收割流
- 今日页（只有 drill 一个块）

**Phase 1**
- Speaking（语音角色扮演 + TOT 复盘回流 bank）
- Paraphrase 一句三说
- 周报自动汇总、红线规则、退休进度条

**Phase 2**
- 月度体检流程 + 激活热力表
- Immersion 看后收尾（复述/台词重构/五句反应）
- 泛词密度趋势（upgrade 槽）

**技术形态**：本地优先 PWA（移动体验 + 桌面贴文本），IndexedDB 镜像 + bank.json 导入导出；LLM 走轻代理（自带 key 或自建），排期逻辑全部本地。不需要账号体系也能用——单机即完整产品。

---

## 8. 明确不做什么（反清单）

- ❌ 识别式卡片（看词选义）——一张都没有
- ❌ 连击 / 排行榜 / 好友打卡 —— "间隔复习靠算法，不靠热情"
- ❌ 无限练习模式 —— 到点就停
- ❌ 一键批量导入词表 —— 收割必须过"认识但用不出"确认
- ❌ 预制通用词库 —— bank 只装用户自己的材料里长出来的语块
- ❌ 社交、徽章、成就墙 —— 数据本身就是奖励
- ❌ 把 LLM 当排期器 —— 排期是确定性代码
